#include <grid.hpp>
#include <characteristic.hpp>

using namespace std;
void calc_rays(GridClass* grid, vector<TangentRay>& ray_vector)
{

    // this is how this function has to end
    // it_char->push_s_mu(s, mu);
}
